---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import { formatDate } from '../../utils/date';

export async function getStaticPaths() {
  const trails = await getCollection('trails');
  return trails.map(trail => ({
    params: { slug: trail.slug },
    props: { trail },
  }));
}

const { trail } = Astro.props;
const { Content } = await trail.render();
---

<BaseLayout title={trail.data.title} description={trail.data.description}>
  <section class="section">
    <div class="container">
      <article class="trail-detail">
        <div class="trail-header">
          <h1>{trail.data.title}</h1>
          <div class="trail-meta-grid">
            <div class="meta-card">
              <strong>Difficulty</strong>
              <span class={`difficulty-badge difficulty-${trail.data.difficulty.toLowerCase()}`}>
                {trail.data.difficulty}
              </span>
            </div>
            <div class="meta-card">
              <strong>Location</strong>
              <span>📍 {trail.data.location}</span>
            </div>
            <div class="meta-card">
              <strong>Date</strong>
              <span>📅 {formatDate(trail.data.date)}</span>
            </div>
            {trail.data.distance && (
              <div class="meta-card">
                <strong>Distance</strong>
                <span>📏 {trail.data.distance}</span>
              </div>
            )}
            {trail.data.duration && (
              <div class="meta-card">
                <strong>Duration</strong>
                <span>⏱️ {trail.data.duration}</span>
              </div>
            )}
            {trail.data.author && (
              <div class="meta-card">
                <strong>Author</strong>
                <span>✍️ {trail.data.author}</span>
              </div>
            )}
          </div>
        </div>

        <div class="trail-content prose">
          <Content />
        </div>

        <div class="back-link">
          <a href="/trails">← Back to All Trail Reports</a>
        </div>
      </article>
    </div>
  </section>
</BaseLayout>

<style>
  .trail-detail {
    max-width: 900px;
    margin: 0 auto;
  }

  .trail-header {
    margin-bottom: var(--space-xl);
  }

  .trail-header h1 {
    margin-bottom: var(--space-lg);
  }

  .trail-meta-grid {
    display: grid;
    gap: var(--space-md);
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  }

  .meta-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    padding: var(--space-md);
    background-color: var(--color-light-gray);
    border-radius: var(--border-radius);
  }

  .meta-card strong {
    font-size: 0.85rem;
    text-transform: uppercase;
    color: var(--color-gray);
    letter-spacing: 0.5px;
  }

  .meta-card span {
    font-size: 1.05rem;
    color: var(--color-dark);
  }

  .difficulty-badge {
    padding: 0.5rem 1rem;
    border-radius: 20px;
    font-size: 0.9rem;
    font-weight: 600;
    text-transform: uppercase;
    display: inline-block;
  }

  .difficulty-easy {
    background-color: #4caf50;
    color: white;
  }

  .difficulty-moderate {
    background-color: var(--color-sand);
    color: var(--color-dark);
  }

  .difficulty-difficult {
    background-color: var(--color-accent);
    color: white;
  }

  .difficulty-extreme {
    background-color: #d32f2f;
    color: white;
  }

  .trail-content {
    background-color: var(--color-white);
    padding: var(--space-xl);
    border-radius: var(--border-radius);
    box-shadow: var(--shadow);
    margin-bottom: var(--space-xl);
  }

  .prose :global(h2) {
    margin-top: var(--space-xl);
    padding-top: var(--space-lg);
    border-top: 2px solid var(--color-light-gray);
  }

  .prose :global(h2:first-of-type) {
    margin-top: 0;
    padding-top: 0;
    border-top: none;
  }

  .prose :global(h3) {
    margin-top: var(--space-lg);
    color: var(--color-secondary);
  }

  .prose :global(h4) {
    margin-top: var(--space-md);
    color: var(--color-accent);
    font-size: 1.1rem;
  }

  .prose :global(ul) {
    list-style: none;
    margin-left: 0;
  }

  .prose :global(li) {
    padding-left: var(--space-lg);
    position: relative;
  }

  .prose :global(li::before) {
    content: '▸';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: bold;
  }

  .prose :global(ol) {
    margin-left: var(--space-lg);
  }

  .prose :global(ol li::before) {
    content: none;
  }

  .prose :global(strong) {
    color: var(--color-primary);
  }

  .prose :global(blockquote) {
    border-left: 4px solid var(--color-accent);
    padding-left: var(--space-md);
    margin: var(--space-lg) 0;
    font-style: italic;
    color: var(--color-gray);
  }

  .back-link {
    text-align: center;
    padding-top: var(--space-xl);
    border-top: 2px solid var(--color-light-gray);
  }

  .back-link a {
    color: var(--color-primary);
    font-weight: 600;
  }

  .back-link a:hover {
    color: var(--color-accent);
  }
</style>

